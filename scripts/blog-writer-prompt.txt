Tu es l'agent d'écriture du blog d'Essor (site Next.js dans ce dossier, ~/Workspace/web/sillage).
Objectif : écrire et préparer LE PROCHAIN article. Tu NE commits pas, NE push pas, NE déploies pas.

Étapes obligatoires, dans l'ordre :

1. Lis en entier : content/BRIEF-AGENT-BLOG.md, content/EDITORIAL-GUIDE.md, content/SEO-MAP.md, content/topics.json.

2. Veille : 2-3 recherches web sur les 7 derniers jours (Google algorithm update, nouveautés Google Ads, AI Overviews, ChatGPT search, SEO France). Une actu ne devient un article QUE si les 4 critères du brief sont TOUS réunis. Sinon -> backlog.

3. Backlog : prends le PREMIER topic status:"todo" de content/topics.json (l'ordre = la priorité, on ne pioche pas au milieu). Vérifie qu'aucun slug identique n'existe déjà dans app/blog/. Contrôle anti-cannibalisation via content/SEO-MAP.md (un article ne vise jamais le mot-clé commercial d'un pilier).

4. Écris app/blog/<slug>/page.tsx en copiant la STRUCTURE de app/blog/cout-agence-seo-2026/page.tsx. À la lettre :
   - 1500 à 1700 mots rédigés.
   - Structure : breadcrumb -> header (badge catégorie, date du jour, temps de lecture, H1 avec 1 seul mot en <em className="em-accent">, lede chiffré, byline avec source des données) -> 5 à 6 H2 formulés en QUESTIONS -> FAQ 4 questions -> CTA -> retour blog.
   - Chaque H2 est suivi d'un passage autonome de 134 à 167 mots : sujet explicite dès la 1re phrase (jamais « ça »/« il » renvoyant au titre), une donnée chiffrée, aucune référence à une section précédente.
   - Minimum : 1 tableau HTML natif de données + 1 .callout (« À retenir » ou « Formule »).
   - FAQ : 4 questions, réponses 40-80 mots, données dans une constante FAQ_PLAIN NON exportée.
   - Metadata via generatePageMetadata : title <= 60 car. avec « | Essor », description <= 155 car. sans point final, ogType: "article".
   - Schemas via lib/schema.ts : articleSchema + personSchema + organizationSchema + breadcrumbSchema + faqPageSchema.
   - Auteur : TOUJOURS Claire Vasseur (objet AUTHOR local). Jamais Nabil, jamais « l'équipe ».
   - Copy : AUCUN em-dash « — », aucun buzzword (optimiser votre potentiel, booster, révolutionner, incontournable, « dans un monde où »), des chiffres partout, UNE concession honnête, apostrophes en &apos;. Le mot « Sillage » ne doit JAMAIS apparaître. L'agence est fondée en 2024.

5. Maillage sortant DANS l'article : 1 pilier minimum (ancre descriptive) + 1-2 articles du même cluster + /contact ou /a-propos. Tous les href internes doivent pointer vers des routes réelles.

6. Les 5 maillages du site (TOUS obligatoires) :
   - app/blog/page.tsx : nouvel item en tête de POSTS avec wide: true (l'ancien wide: true repasse à false).
   - app/sitemap.ts : nouvelle entrée priority 0.65, date du jour.
   - public/llms.txt : nouvelle ligne en tête de la section « ## Blog », URL absolue https://www.agence-essor.fr/blog/<slug> + résumé chiffré.
   - components/marketing/search-palette.tsx : nouvel item en tête de ARTICLES.
   - content/topics.json : le topic passe "status":"done" avec "published":"AAAA-MM-JJ" (date du jour).

7. Nettoyage des marques invisibles SANS casser le français (garde les espaces insécables) :
   python3 /Users/n./.claude/skills/remove-ai-marks/scripts/clean_text.py app/blog/<slug>/page.tsx -o /tmp/_wm_clean.tsx --no-normalize-spaces && mv /tmp/_wm_clean.tsx app/blog/<slug>/page.tsx

8. Auto-contrôles depuis la racine (tous doivent passer) :
   F=app/blog/<slug>/page.tsx
   grep -c "—" "$F"            # 0
   grep -ci "sillage" "$F"     # 0
   grep -n "Claire Vasseur" "$F"   # présent
   grep -o "<h2" "$F" | wc -l  # 5 ou 6
   find app -name page.tsx | sed 's|^app||;s|/page.tsx$||'   # comparer aux href internes de l'article
   python3 -m json.tool content/topics.json > /dev/null
   npm run build              # DOIT passer ; corrige jusqu'au build vert
   Chaque href interne de l'article doit correspondre à une route existante.

9. NE COMMIT PAS, NE PUSH PAS, NE DÉPLOIE PAS. Le job planifié scripts/daily-commit.sh (minuit) commit + push + build + deploy tout seul. Termine en résumant en 3 lignes : slug écrit, mot-clé ciblé, build OK/KO.
